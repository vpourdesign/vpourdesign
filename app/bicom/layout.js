export const metadata = {
  title: 'Grille tarifaire — BICOM × V pour Design',
  description: 'Document de travail privé.',
  robots: {
    index: false,
    follow: false,
    nocache: true,
    googleBot: { index: false, follow: false, noimageindex: true },
  },
};

export default function BicomLayout({ children }) {
  return children;
}
